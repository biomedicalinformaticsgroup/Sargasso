#!/usr/bin/env bash

set -o nounset
set -o errexit

SPECIES=$1
SAMPLES=$2
OUTPUT_DIR=$3

for species in ${SPECIES}; do
    for sample in ${SAMPLES}; do
        ln -s ~/projects/species_separation/results/${sample}.${species} ${OUTPUT_DIR}/
    done
done
